# ================================================================================
# CIFAR-100 Dataset Configuration
#
# Configures OmniFed DataModule for CIFAR-100 fine-grained object classification.
# Uses torchvision dataset with standard normalization and data augmentation.
#
# Train sharding matches CIFAR-10 (unique trainer shards; server has no train).
#
# Docs:
# - https://pytorch.org/vision/stable/generated/torchvision.datasets.CIFAR100.html
# - https://www.cs.toronto.edu/~kriz/cifar.html
# ================================================================================

defaults:
  - base
  - _self_

_target_: src.omnifed.data.federated_vision.build_federated_vision_datamodule

# ─────────────────────────────────────────
# Training Data
# ─────────────────────────────────────────
train:
  dataset:
    _target_: torchvision.datasets.CIFAR100
    root: ${oc.env:OMNIFED_DATA_DIR,/tmp}/cifar100
    train: true # Use training set
    # target_transform: null  # Optional transform for targets
    download: true
    transform:
      _target_: torchvision.transforms.Compose
      transforms:
        - _target_: torchvision.transforms.ToTensor
        - _target_: torchvision.transforms.RandomHorizontalFlip
          p: 0.5
        - _target_: torchvision.transforms.RandomCrop
          size: 32
          padding: 4
        - _target_: torchvision.transforms.Normalize
          mean: [0.50707516, 0.48654887, 0.44091784] # CIFAR-100 specific mean
          std: [0.26733429, 0.25643846, 0.27615047] # CIFAR-100 specific std

# ─────────────────────────────────────────
# Evaluation Data
# ─────────────────────────────────────────
eval:
  dataset:
    _target_: torchvision.datasets.CIFAR100
    root: ${oc.env:OMNIFED_DATA_DIR,/tmp}/cifar100
    train: false # Use test set
    # target_transform: null  # Optional transform for targets
    download: true
    transform:
      _target_: torchvision.transforms.Compose
      transforms:
        - _target_: torchvision.transforms.ToTensor
        - _target_: torchvision.transforms.Normalize
          mean: [0.50707516, 0.48654887, 0.44091784] # CIFAR-100 specific mean
          std: [0.26733429, 0.25643846, 0.27615047] # CIFAR-100 specific std
