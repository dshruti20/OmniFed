# @package _global_
# PI centralized — sync grad + QSGD over TorchDist (CIFAR-10, ResNet-18).

defaults:
  - /old_way/test_pi_centralized_sync_grad_cifar10_resnet18_torchdist
  - _self_

topology:
  local_comm:
    compressor:
      _target_: src.omnifed.communicator.compression.quantization.QSGDQuantCompression
      device: cuda
      bit_width: 4
