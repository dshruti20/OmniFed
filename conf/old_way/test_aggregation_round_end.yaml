# @package _global_
# ================================================================================
# Round-End Aggregation Test Configuration
# Tests standard round-level aggregation frequency.
# Inherits from centralized torchdist base and overrides aggregation timing.
# ================================================================================

defaults:
  - /old_way/test_fedavg_centralized_torchdist # Base centralized configuration
  - override /algorithm/schedules/aggregation: round_end # Override aggregation schedule
  - _self_

# ─────────────────────────────────── Algorithm Overrides
algorithm:
  max_epochs_per_round: 4

# ─────────────────────────────────── Engine Parameters
global_rounds: 3 # see ./conf/base.yaml for all options
