# @package _global_
# ================================================================================
# Standard Evaluation Test Configuration
# Tests standard FL evaluation pattern (start + post-sync + end).
# Inherits from centralized torchdist base and overrides evaluation timing.
# ================================================================================

defaults:
  - /old_way/test_fedavg_centralized_torchdist
  - override /algorithm/schedules/evaluation: standard
  - _self_
