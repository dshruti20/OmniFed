# ================================================================================
# FedAvg Algorithm Configuration
#
# The original federated learning algorithm.
# Simple average of all client model updates (payload=params).
# For per-batch gradient averaging use algorithm: fedsgd (same loop).
# Task knobs (not new algorithm files): optimizer sgd|adamw; HF dict batches use LM loss.
# ================================================================================

defaults:
  - base
  - _self_

# NOTE: Check constructor signature for complete parameter list, defaults, types, and docs.
_target_: src.omnifed.algorithm.FedAvg
